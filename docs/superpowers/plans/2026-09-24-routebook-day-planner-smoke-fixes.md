# 按天行程本 v1 冒烟（Codex）问题修复（2026-09-24）

> 执行者须知：先读主计划 `docs/superpowers/plans/2026-09-23-routebook-day-planner.md` 顶部硬约束。Codex 浏览器冒烟报告在 `scratch/codex-smoke-routebook/report.md`（证据 `api-events.json`、`mutation-requests.jsonl`、截图）。可改 `app/(authed)/me/routebooks/**`、`components/navigation/**`、`lib/i18n/locales/*.json`、`tests/routebooks/**`；后端仅限 `lib/routeBook/handlers/places.ts`（S3 若需要）。不 push、不 migrate。完成标准：typecheck 0 错；`npx vitest run --project jsdom tests/routebooks`、`npx vitest run tests/i18n tests/routeBook` 全绿；`npm test` 全绿；简短中文汇报。路径相对 `app/(authed)/me/routebooks/[id]/`。

- **S1 [❌ #5] 桌面跨天拖拽落到「未安排」**：把条目拖到**折叠的** Day 2 区块中心，`POST items/reorder` 发的是 `dayId=null`。排查 `hooks/useTripDnd.ts` 的 `onDragEnd` 解析：折叠天的 droppable（`DayBlock.tsx:90` `day:<id>`）是否覆盖整个区块；`over` 落在天标题/折叠区时必须解析为该天末尾；碰撞检测在跨容器时用 `pointerWithin` 优先、`closestCenter` 兜底（或 `closestCorners`），不要让「未安排」容器在指针不在其内时被选中。jsdom 测试：`over = { id: 'day:<day2>' }`（折叠）→ reorder 参数 `dayId = day2`。
- **S2 [❌ #11] 新建备注后本地残留临时 id**：流程是 `POST /items` → 用响应 `items` 替换目标天 → 再 `PATCH` 正式 id 写 icon/color。现在 PATCH 虽然成功，本地状态里那条仍是 `temp-…` id 且样式未更新，随后「编辑备注」PATCH 到 temp id → 404。修 `components/DialogsHost.tsx` + `hooks/useItemMutations.ts`：POST 成功后一律用响应里的 `item`/`items` 替换临时条目（含 id、icon、color、timeStart），PATCH 的响应也合并进状态；撤销环里的引用同步改为正式 id。测试：`tests/routebooks/DialogsHost.note.test.tsx` 加「创建后立即编辑用的是正式 id」。
- **S3 [⚠️ #9] 新建自定义点应立刻上地图**：保存自定义点后自动创建一条 `kind:'place'` 条目——有选中天则加到该天末尾，否则加到「未安排」（空心标记）；从地图右键创建同理。若后端 `POST /places` 只建 place，就在前端紧接着 `POST /items`（两步）；`PlaceEditorDialog` 编辑模式不新增条目。测试覆盖两种目标。
- **S4 [⚠️ #14] 缺少行程日期编辑入口**：`components/DetailChrome.tsx` 面包屑标题旁加「日期」按钮 → 小弹窗：开始日期（`<input type="date">`）+「清除日期」→ `patchBook({ startDate })`（ISO 或 null）；保存后各天 `date` 由服务端重算（响应里带 days，用它更新）。移动端在 `DaySummaryBar` 抽屉里同样提供。三语文案 `routebook.dates.*`。测试：提交日期后 PATCH 体正确。
- **S5 [⚠️ #12] 点位图片走 R2 公共域**：`hooks/usePointPreviews.ts:77,98` 直接用了接口返回的原始 `image`（`image.anitabi.cn`，浏览器 ORB 拦截）。改为与 `/map` 相同的解析：找 `features/map/anitabi/media.ts` / `lib/anitabi/imageProxy.ts` 里面向浏览器的点位图 URL 构造函数（R2 公共域 `NEXT_PUBLIC_MAP_IMAGE_R2_PUBLIC_BASE` → 代理 → 直连 的候选梯）并复用（只 import，不改那边）；标记缩略图、详情卡大图、时间线缩略图都受益。测试：给定原始 anitabi URL，产出的 src 以 R2 公共域开头。
- **S6 [nit] 快速重复加入同一点后撤销两次出现 400**（01:14:43，`items/<id>` 400）：排查 `useItemMutations` 的撤销栈——同一 `pointId` 同天幂等返回已有条目时不要再 push 一条「删除该条目」的撤销记录（否则第二次撤销删除已不存在的条目）。加测试。
- **S7 [说明，不改]** #16 沉浸模式点位站没有「到达·下一站」是设计如此（作品点位靠打卡/跳过推进，自定义点才有到达），距离 `--` 是测试环境无定位。
