/**
 * 空回合守卫：推理模型的 reasoning 计入 completion 预算，输出预算被思考
 * 耗尽时（finish_reason=length），模型会返回一条"正文为空、无工具调用"
 * 的 assistant 消息。旧实现把这种回合当正常结束——界面毫无输出，重试
 * 仍撞同一堵墙。守卫在循环层识别空回合：第一次注入纠正指令重试一次，
 * 仍为空则显式报可恢复错误，绝不把空 assistant 落库。
 */

/** 重试时注入的纠正指令（只进模型消息，不落库、不下发前端） */
export const EMPTY_TURN_RETRY_INSTRUCTION =
  '你上一轮没有输出任何文字或工具调用（输出预算已被思考耗尽）。' +
  '请大幅缩短思考，直接给出下一步：要么发起工具调用（本轮不超过 5 个），' +
  '要么用一两句话回复用户。'

/** 重试后仍为空时，下发给前端的可恢复错误文案（前端会附重试入口） */
export const EMPTY_TURN_ERROR_MESSAGE =
  '模型这一轮的思考超出了输出预算，没有产出内容。' +
  '请再发一条消息继续；如果行程很大，可以先让我只排前几天。'
