---
线上验证结果（2026-01-24 抽样抓取）
| 检查项 | 状态 | 备注 |
|--------|------|------|
| JSON-LD：Organization.logo | ✅ 已修复 | 现在输出 "logo":"https://seichigo.com/brand/app-logo.png"（URL 字符串） |
| JSON-LD：BlogPosting.publisher.logo | ✅ 已修复 | 帖子页 publisher 内含 logo 字段 |
| TVSeries 作品 schema | ✅ 正常 | /anime/btr 输出了 @type: TVSeries 结构化数据 |
| hreflang 标签 | ⚠️ 存在但属性为 camelCase | hrefLang="zh" 而非 hreflang="zh"。多数浏览器兼容，但部分工具会警告。 |
| OG / Twitter tags | ✅ 正常 | 动漫页、帖子页均正确输出 og:title/og:image/twitter:image |
| Canonical | ✅ 正常 | 首页 canonical 指向 https://seichigo.com |
| HTTP Cache-Control | ⚠️ no-cache | Vercel 响应 cache-control: private, no-cache...，每次请求都打后端，TTFB 高 |
---
仍需优化的 SEO/ICU 事项（按优先级）
P0（校验/信任问题）——已解决 ✅
- Organization.logo 类型错误 → 已改为 URL 字符串
- BlogPosting.publisher.logo 缺失 → 已添加
P1（性能/CWV）——待处理 🔴
| 问题 | 影响 | 建议 |
|------|------|------|
| TTFB 高（多页 > 1 秒） | LCP / TTFB 指标差 | 开启 ISR/Edge Cache；优化 DB 查询；Vercel 设置 s-maxage |
| LCP 图片未 preload | LCP 延迟 | 首页/帖子页的 cover 图添加 priority 或 <link rel=preload> |
| above-fold 图片 lazy | LCP 延迟 | 确保首屏图片不是 loading=lazy；首页 logo 已有 priority，但 cover 图需检查 |
| CLS：图片无尺寸 | CLS 分数差 | 给 cover、地图等 <img> 加 width/height 或 aspect-ratio |
P2（SEO 卫生）——低优先但仍需做
| 问题 | 建议 |
|------|------|
| hrefLang 属性大小写 | Next.js 内部用 camelCase，浏览器兼容；如 SEO 工具持续报警，考虑 post-render 注入自定义 head 逻辑 |
| meta-in-body 警告 | 部分页面（/anime/*, /en, /about）被检测到 meta 标签出现在 <body>；可能是 streaming 导致，需调研 Next.js head 渲染顺序 |
| canonical redirect chain | /posts/你的名字-... 被重定向到 %E4%BD%A0%E7%9A%84%E5%90%8D%E5%AD%97-...；把 canonical 直接设成 encoded URL |
| city 非语义 slug | city-91bf319d → 需 DB 迁移 + 301 重定向（BLOCKED，需产品决策） |
---
本地代码（lib/seo）检视
| 文件 | 状态 |
|------|------|
| lib/seo/globalJsonLd.ts | ✅ logo 已修为 string |
| lib/seo/jsonld.ts | ✅ publisher.logo 已添加 |
| lib/seo/alternates.ts | ✅ hreflang helper 正常；输出交给 Next metadata |
| lib/seo/tvSeriesJsonLd.ts | ✅ TVSeries schema 正常 |
---
本次仓库侧已完成（待线上验证）：

- 缓存/TTFB：主页/索引页切换为 `force-static` + `revalidate`，sitemap 设置 `revalidate=3600`
- LCP/CLS：关键卡片封面与地图预览补齐 `width/height`，首屏图片去掉 lazy（LCP 候选）
- 图片体积：对 `/assets/*` 封面统一追加 `?w=&q=` 变体以减少体积
- 可访问性：MDX 图片渲染补齐 `alt` 属性（缺省为空字符串）
- 结构化数据：全局 JSON-LD（WebSite/Organization）移动到 `<head>`（`app/head.tsx`）
- 元数据：补齐/拉长索引页与资源页 meta description（本地 `npm run seo:audit` 已 0 warnings）

仍需线上验证：

- `content/meta-in-body` 是否消失（Next.js streaming 相关）
- `schema/json-ld-valid` 是否仍被 SquirrelScan 判定失败（可能是工具误报或位置解析问题）
- Core Web Vitals（TTFB/LCP/CLS）在部署后是否显著下降
---

2026-01-24 追加验证（本地执行）

- Production `npm run seo:audit -- --base-url https://seichigo.com`
  - `/anime/*` 与部分 `/posts/*` 当前为 500（导致 canonical/OG/Twitter/JSON-LD 全部缺失属于 500 的连带症状）
- Production Squirrel（v0.0.17, 23 pages）
  - overall 59 (F)，Structured Data 0
  - `schema/json-ld-valid` 仍失败（city/resources 多页），但 report 未给出具体字段路径（items 为空）
  - `content/meta-in-body` 未在本次 squirrel json 中出现（采样页 `/en`/`/about`/`/anime` 的 `<meta>` 均在 `<head>`）

本地修复（待部署）

- Global JSON-LD 不在 production HTML（`/en` 页面 `application/ld+json` count=0）。
  - 已改为在 `app/layout.tsx` 用 `<Script type="application/ld+json" strategy="beforeInteractive">` 注入到 `<head>`。
  - `app/head.tsx` 已移除（production 侧未生效，怀疑被忽略/未路由）。
- `schema/json-ld-valid` 的适配修复：
  - `BreadcrumbList.item` 改为 `{ "@id": url }`
  - `ItemList.itemListOrder` 改为 `https://schema.org/ItemListOrderAscending`
- 缓解 anime/post 聚合页潜在 500（DB 记录字段缺失导致运行时 crash）：
  - `lib/posts/defaults.ts`：PublicArticleRepo 的 `listByStatus` 改为最小字段 select + 归一化，避免 `contentHtml`/`contentJson` 未选中导致 downstream `.replace/.trim` crash。

本地验证（已完成）

- `npm run seo:audit -- --base-url http://localhost:3000`：errors=0 warnings=1（/resources description 偏短）
- `npm test`：pass
- `npm run build`：pass
